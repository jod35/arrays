let names=[ 'Jerusha', 'Jerry', 'Jannet', 'Jona', 'Jordan' ];

names.forEach(
    (name)=>{
        console.log(`${name} has a length of ${name.length}`);
    }
)

// this function calls a callback on each of the elements in the array